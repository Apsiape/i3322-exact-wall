# Blind reconstruction brief: quantitative I3322 dimension necessity

## Source boundary

Use only the 19 production theorem sources frozen in `source-manifest.json`,
the standard mathematical facts named in the preregistration, and newly
written code.  Do not inspect the production dimension-bound assembly in
foundational sprints 1231, 1233, 1234, or 1235 before adjudication.

## Target

Let `Q_d` be the optimum normalized I3322 value over tensor-product quantum
strategies with both local dimensions at most `d`, and let `q_*` be the exact
wall value supplied by the allowed sources. Determine whether those sources
imply computable constants `c>0` and `Gamma<infinity`, independent of `d`,
such that

```text
q_* - Q_d >= c d^-4 Gamma^-d
```

for every `d>=1`.

The reconstruction must price the initial retained mass, near-fixed and
inactive-tail charges, all moving frames, recurrence residuals, terminal
energy, rank, ancestry, and every reuse multiplicity. It may not assume a
density, global orbit disintegration, fibre partial isometry, simultaneous
top-`d` selection, fixed positive gap on the whole inactive sliver, a bound on
the sum of all chain lengths, a coarse-to-fine amplitude identification, or a
multiplicity-free all-frame sum.

