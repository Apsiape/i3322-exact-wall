# Round-3 W1–W10 execution ledger

| ID | v5 execution | Status |
|---|---|---|
| **W1** | Convex minorant is now built on `D=(-1,1)`. Added `C>=g(0)>0` from feasibility at `u=0`; deleted all reliance on an endpoint-excluded compact source interval or wing receipt. | **EXECUTED** |
| **W2** | Replaced the noncompact chord argument for envelope reduction by the affine-minorant proof using `m=inf(C-uy)`. | **EXECUTED** |
| **W3** | Replaced the noncontact chord-endpoint argument by the direct lifting contradiction `max(H,ell+epsilon)`. | **EXECUTED** |
| **W4** | Replaced “active iff `u in partial H(x)`” by the used implication; derived `H(x)=C(x)` at active points. | **EXECUTED** |
| **W5** | Published strengthened contact theorem: at every contact point `C'_-=H'=C'_+`; strict downward corners are inactive. | **EXECUTED** |
| **W6** | Added the explicit symmetric spectral cutoff and norm-convergence paragraph to the limiting-weld receipt. | **EXECUTED** |
| **W7** | Added exact signed public statement and binding wording: normalization, certified interval only, asymmetry distinction, verifier scope, nonclosure vs spatial separation. | **EXECUTED** |
| **W8** | Removed contradictory v4 OPEN-status documents, dead schemas, validators, branch verdicts and supersession chain from the operative package. Added a retirement ledger. | **EXECUTED** |
| **W9** | Added `artifacts/README.md`; every script banner states that it checks algebraic identities or exact arithmetic only and does not verify the theorem. | **EXECUTED** |
| **W10** | Replaced `Q_127`/Sprint-1288 wording with Sprint 1292’s exact dimension-255 lower receipt. | **EXECUTED** |

## Final package status

Theorem (N) is promoted. The package does not promote spatial attainment or
`C_qs \\ C_q` separation.
