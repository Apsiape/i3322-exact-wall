# Sprint 1231 pre-registration

Register the theorem form

```text
q_*-Q_d >= c_* d^-4 Gamma^-d,
Gamma=(20*78/5)^4=312^4=9475854336,
c_*>0 independent of d.
```

`c_*` may be defined through the positive compact inactive-strip gap and the
already-certified universal constants; a decimal optimization is not
required. The proof must explicitly display:

1. near-fixed absorption;
2. inactive-strip payment;
3. moving-grid loss;
4. retained initial drift mass;
5. recurrence and terminal-exit energy; and
6. the final large-loss/small-loss case split.

