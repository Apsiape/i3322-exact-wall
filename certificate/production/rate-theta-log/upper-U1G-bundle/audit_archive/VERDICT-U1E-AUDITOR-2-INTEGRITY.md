# U1E re-gate — Auditor 2 (integrity): J2 FAIL (KILL); gate DENIED. THE BUNDLE AUTHOR WAS THE AUDIT TRACK — the independence layer caught its own gatekeeper.

2026-08-07. Six fault injections all fired; manifest 29/29 exact;
change enumeration verified honest; Theorem-S re-anchoring reproduced
end-to-end ("F3 is dead"); H5 confirmed a REAL predecessor check
("F20 is dead"); G1 restoration byte-exact ("F5/F11 dead"). "This is
competent engineering." AND: it does not promote.

## The KILL (F-6) — the U1 KILL was RELOCATED, not discharged

(a) The retained Cesàro receipt's line 28 still names the killed
q_ret route as the strictness supplier — inside my declared live
chain. (b) The receipt defines rho as a QUOTIENT of two different
multipliers at two different labels (r_B,mult(α)/r_A,mult(x+)); my §3
describes a PRODUCT and collapses to r_B(−t*)² with r_B never defined
— kill #12's governed objects are ENGAGED, not bypassed. (c) The
collapse needs g(t*)g(−t*) = b² — asserted as "reciprocity" with NO
receipt, the same structural family as the killed identity. [MY REPAIR
ROUTE IDENTIFIED POST-VERDICT: the identity IS receipted — zero-set
localization K=1 holds at the carrier's labels because the carrier is
a full-zero orbit (CRITICAL_ZERO_SET_REDUCTION §5-6, in v28.1 deps) +
b even ⟹ r(−t*)² = g(−t*)/g(t*). Conditional, lawful, distinct from
the killed unconditional identity. Must be DERIVED in the proof with
the receipt quoted, and the quotient-vs-product reconciliation
requires READING the receipt text first — the file+line rule applies
to me too.] (d) The four §3 authorities (equality-module, sextic,
wall-comparison selection, q* bracket) are PROSE NAMES with no file,
no hash — anchored strictly less than the RHO_Q document whose
unanchored status was the original KILL.

## Majors against my build

F-7: SELF_AUDIT_U1.md retained sealed, certifying the killed route as
PASS inside my bundle. F-8: small-d demotion incomplete
(U1_GUARD_RESULTS.txt with the killed tautological banners still in
guards/; orphan .pyc manifest-sealed and omitted from my change list).
F-1/2/3: "the scalar-orbit document is NOT consumed" is FALSE — the
promoted certificate doc IS the scalar-orbit-extraction document (its
title says so); its own header still reads PROMOTION CANDIDATE (only
the sibling STATUS.json was reconciled — PUBLIC-REPO FIX NEEDED, same
class as the Mghirbi fix); its §10 rests on the disclosed-risk §§6-9,
undisclosed by me. F-4: my banner's "NO raw predecessor text" is
false (dependencies carry full predecessor docs). F-11: my two-routes
note is in the live chain with contradictory numerics + an open R2
obligation. F-10: I wrote "9e-11" twice in the proof while my own
guard printed 8.907e-13 — known at seal time.

## Minors

F-12 retraction-block exemption is launderable (no check that blocks
contain retraction language — assertion laundering possible; F23's
fix relocated the problem); F-13 STATUS regenerating-outputs claim
false via the shipped demoted guard; F-14 H5 hardcoded literals +
soft-skip; F-15 H2 scope narrower than the claim it certifies (the
hole F-6a/F-7 passed through); F-16 PART B caveat mis-wired; F-17
graph absolutism (transitively false via Theorem-S §14); F-18 stale
README/receipt/patch pointers.

## Discharge table (from the verdict)

DISCHARGED: F3, F5, F11, F16, F19, F20, F23-mechanism, F24,
F14-int-edge. PARTIAL: F10 (carried but "BYPASSED" inaccurate), F15
(new false claim), F17, F22 (symptom fixed, design rule not adopted).
NOT: F6/E1 (unanchored), F21 (disclosed-only, acceptable at [V]).

## The process lesson (auditor's own words)

"F-6, F-7 and F-8 are all cases where a claim about the bundle was
certified by a guard whose scan scope was drawn narrower than the
claim... the hygiene guard's LIVE list must coincide with the
banner's LIVE CHAIN definition." Banked: SCOPE COINCIDENCE RULE —
every guard's scan set is generated from the authority banner's live
list, never hand-maintained.
