# U1 upper gate — Auditor 2 (integrity): I1 FAIL (one KILL); gate DENIED on these surfaces

2026-08-07. Engineering strong (22/23 manifest exact + 1 newline-only;
guards reproduce in two environments; NINE fault injections all fired;
sanitized ledger genuinely auditable and drops nothing — walked all 12
v28.1 kills; upper proof cleanly quarantined from the lower bound;
two-ladder alarm resolved on a THIRD engine). What fails is PROVENANCE
ON THE LOAD-BEARING STEPS.

## The KILL (F7)

The strictness step ρ± < 1 — sole source of κ_eff > 0, hence the
entire rate — rests on RHO_Q_IDENTIFICATION.md, predecessor material
EXPLICITLY STAMPED "cannot promote D_upper or Θ(log)", carrying two
undischarged assumptions: (a) that r_{A,mult}/r_{B,mult} are the
NORMALIZED gains F(q) consumes — v28.1 permanent kill #12 forbids
raw-gain substitution and NO RECEIPT rules it out (the ledger pattern
again: the kill governing the new proof's center is the one not
carried forward — same shape as kill #2 resurfacing in CME); (b) that
the endpoint limit is a closed return sector satisfying (1.1)-(1.2)
AT Q = S — nowhere established. If either fails: only ρ ≤ 1 survives,
κ_eff = 0 admissible, no rate at all.

## The MAJORs

F6 — OPEN RECEIPT E1 SILENTLY CLOSED: the G1 extract ADDS a sentence
its hash-anchored source lists as open ("endpoint labels lie in one
compact subinterval") — G1 proves Z ⋐ (−1,1)²; the step from the
locus to the four carrier-orbit LIMITS needs the unstated premise
limits ∈ closure(Z). [NOTE: F6 and F7(b) are the SAME missing object
— endpoint limits are full-zero limits; one receipt discharges both
and likely supplies the component equations at Q = S with normalized
g-ratio gains. The repair is one theorem, not three patches.]
F3 — Theorem (S) anchor is SELF-REFERENTIAL (hash of U1's own copy;
file exists nowhere in the sealed predecessor; the guard "verifies" it
tautologically). Real provenance EXISTS (public repo v3.1.0 + DOI) —
cite it. F5 — the "G1 cleanup" is a 184→23-line extract that DELETED
THE RETRACTION NOTICE, because — F23 (METHODOLOGICAL, new pattern
class) — THE LITERAL BLACKLIST CANNOT DISTINGUISH ASSERTION FROM
RETRACTION: the hygiene guard scans the whole bundle incl. provenance
trees, making a faithful predecessor ledger impossible to ship, then
certifies the sanitization it mandated. Fix: scope scans to the live
chain; exempt marked retraction blocks. F10 — kills #12 and #2 not
carried. F11 — G1 edit not ledgered. F15 — banner falsely claims
predecessor text is preserved (contradicts its own README). F20 —
one banner overstates (anchor "verification" is self-consistency).
F21 — fixture payload hash never recomputed (11+12 of 255 entries).
F22 — newline determinism (one-line fix; design rule: regenerating
outputs must not be manifest-sealed). F14 — spurious THEOREM_S → G1
edge; S > 1/4 usage understated. F16/F17/F24 minors (state the gate
rule; dead manifest clause; window rule = argmax, verified sound and
non-cherry-picked).

## Verified positives

Quarter ceiling byte-identical; 5/6 anchors resolve to real
predecessor artifacts; truncation extract faithful (no strengthening;
d = |I| route swap legitimate and ledgered); ledger completeness
CONFIRMED against the real v28.1 ledger; corollary-only lower
consumption verified; guards fail-capable (9/9 injections);
TWO-LADDER RECONCILIATION verified with mechanism: bundle ≤ external
at every common d; three slack sources (fixed profile vs optimized;
restricted vector vs Perron; PV-padding vs projector completion —
external PART B is NOT a second engine for the bundle's values, only
PART A is, and it is stronger); the parity split identified precisely
as WHICH matching absorbs each severed singleton (M1/M1 > mixed >
M0/M0), bundle's even-d arithmetic confirmed to 6e-61 on a third
engine.

## Blockers

1 (KILL) discharge F7's two assumptions or restore the Sprint-1198
route with receipt; 2 receipt E1; 3 real Theorem (S) provenance;
4-7 cheap (banner, ledger entries, guard scoping, newline).
