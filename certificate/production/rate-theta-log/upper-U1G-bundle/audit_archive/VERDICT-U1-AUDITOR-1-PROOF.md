# U1 upper gate — Auditor 1 (proof): P1/P2/P3 FAIL, P4 PASS, P5 FAIL — DOES NOT PROMOTE

2026-08-07. Convergent with auditor 2 on the central KILL.

## The KILL (F1 = auditor-2 F7, independent convergence)

The quarter-ceiling hypotheses are NEVER DISCHARGED for the endpoint
datum: (1.1)-(1.2) not exhibited, (x,u) never constructed, Q = S
asserted in a subordinate clause, gains never identified with the
Cesàro multipliers. COUNTERMODEL SHAPE: λ_j = c/|j| is a legitimate
ℓ² carrier with ρ± = 1, κ = 0 — satisfies everything proved before §3
and yields POLYNOMIAL D_upper. The gap is the whole theorem. Same
undischarged assertion as the predecessor's historical doc; ledger
item 5's "repair" fixed the orientation, which was never the gap.

## Majors

F2 — Theorem (S) consumption EXCEEDS the signed statement's license:
diagonal Schmidt form, positive λ, labels, Jacobi recurrence, and the
alternating rank-one block structure all come from
CURRENT_S_SPATIAL_ATTAINMENT_BY_SCALAR_ORBIT (§§9-11) — NOT in the
live chain, not anchored, self-declared "promotion candidate". F9 —
error decomposition omits the RETAINED endpoint diagonal (coefficient
jumps to 1 at a site inside I; λ_a² in neither T_I nor B_I; repair
available: λ_a² ≤ R_max·B_I with R_max from G1 — true, unwritten).
F10 — C_B never derived (four-line sketch under an unverified
linear-combination hypothesis; no operator norms; no named constant).
F11 — (4.3) mis-cites the Cesàro receipt for T_I (receipt covers B_I
only; the extension is true — auditor verified the indexing, no
factor-2 error — but underived). F13 — MY COMMISSION PREMISE WAS
FALSE: no trichotomy/superexponential classification exists in any
source (zero grep hits both bundles); the worker's U1a report invented
it; the branch is excluded via ρ > 0, not handled. [PROTOCOL: worker
stage-reports are claims, not receipts — verify quotes against files.]
F14 — S_d and D_upper(ε) NEVER DEFINED (the boxed claim is not
well-formed — same defect class as v19's F-14). F20 — small-d guard's
segments are the UNDISCLOSED per-d argmax (verified: rank 1 of N at
every d); odd chain not nested; parity story partly artifact of
selection on a 12-site excerpt. F21 — tautological banners in the
small-d guard (normalization identity computed against itself;
constant-True projector sub-checks). F22 — fixture hash never
recomputed.

## Verified sound (preserve)

Hygiene P4 in full (0 hits all retired literals; Θ conditional
everywhere; scope fence correct incl. declining the source's
Schmidt-rank boxing). The λ_j → 0 ratio argument (genuine repair of
the square-summability shortcut). G1 conditional-only (independent
grep). Exact d = |I| with the +3 padding genuinely eliminated
(real improvement, correctly ledgered). 140-decimal exact-rational
interval arithmetic (no float in the value path; fail-capable).
Parity claims match code. Classical max = 0 and qubit/qutrit max =
1/4 independently reproduced; exact rational gap S_- − F(889/1000)
re-verified.

## Blockers

B1 (KILL) exhibit (1.1)-(1.2) at the endpoint datum with Q = S and
gains identified; B2 carrier structure into the live chain with real
anchor + receipt (or narrow §1 and lose §5's hypotheses); B3 charge
the retained endpoint diagonal + derive C_B; B4 receipt or derive the
T_I bound; B5 define S_d and D_upper, resolve the trichotomy
discrepancy; B6 write the two-tail algebra/every-d step/limsup form;
B7 guard disclosures or demotion.
