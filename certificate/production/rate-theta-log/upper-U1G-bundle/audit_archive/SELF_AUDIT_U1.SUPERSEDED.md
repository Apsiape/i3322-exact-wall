SUPERSEDED STAMP (2026-08-07): this self-audit certified the U1
route, which the U1 gate KILLED (its PASS table includes the killed
strictness step). It is a historical record of a superseded claim,
not a verdict and not current. Self-audits are claims, not verdicts
(gate rule).

# U1 Self-Audit — I3322 logarithmic upper gate

**Scope:** Stage U1 only. This is an internal package/inference audit, not the external promotion verdict.

## Claim under audit

At local Hilbert-space dimension scope,
\[
D_{\rm upper}(\varepsilon)=O(\log(1/\varepsilon))
\]
with existential positive coefficient `1/kappa_eff`, where
\[
\kappa_{\rm eff}=\left(\kappa_-^{-1}+\kappa_+^{-1}\right)^{-1}>0.
\]
No numerical rate coefficient is carried by U1.

The already promoted lower result is consumed only for the conditional corollary
\[
D(\varepsilon)=\Theta(\log(1/\varepsilon)),
\]
which is not promoted unless the external U1 gate passes.

## Gate checklist

| Gate | Internal verdict | Receipt |
|---|---|---|
| Exact spatial carrier exists | PASS | promoted `THEOREM_S_SIGNED_PUBLIC_STATEMENT.md` |
| G1 endpoint input | PASS | compactly interior full-zero locus; conditional endpoint argument only |
| `rho_pm <= 1` | PASS | `lambda_j -> 0` plus outward two-step ratio convergence |
| `rho_pm = 1` excluded | PASS | explicit `rho <=> q_ret` orientation + neutral quarter ceiling |
| Endpoint-Cesaro rate | PASS | existential `kappa_pm>0`, balanced `kappa_eff>0` |
| Truncation legitimacy | PASS | normalized retained interval, two named severed bonds, endpoint projectors, six exact projections |
| Local-dimension typing | PASS | `d=|I|`; no Schmidt-rank reinterpretation |
| Small-d mechanical guard | PASS | `d=3..8`, exact/rational projection checks, 140-digit interval arithmetic, parity-resolved rise below certified window |
| Retired route hygiene | PASS | no killed global endpoint identity in live chain; no retired numerical upper coefficient anywhere in U1 text bundle |
| Lower/Theta authority typing | PASS | lower `[P]`; Theta conditional on U1 promotion |
| Stage boundary | PASS | U2/U3/U4 not started |
| Provenance | PASS | v28.1 bundle/ledger and source receipts SHA-256 anchored; sanitized ledger comparison supplied |

## Guard replay

`guards/U1_GUARD_RESULTS.txt` records stock-Python and `PYTHONUTF8=0 LC_ALL=C` runs of both U1 guards. All assertions pass.

## Claim boundary

The small-d guard is a mechanical construction/algebra smoke test, not a theorem verifier and not evidence for a numerical asymptotic coefficient. The analytic logarithmic rate rests on the audited proof and its named dependencies.

## Internal determination

\[
\boxed{\textbf{U1 READY FOR EXTERNAL UPPER-ONLY BLIND GATE.}}
\]

No U2 work has been started.
