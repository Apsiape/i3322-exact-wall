# U1E Correction and Supersession Ledger

Predecessors: sealed U1 bundle (archive SHA-256
44907d3ff9cbd8711d845093f2dea921bbb0e32a14613d241ac89dbd3724aa84,
detached manifest 0308b3f10e829fedbcd6a8ef4a8ae5e158144aa032142062
bf29bb6de3d1bc52) and, through it, the sealed v28.1 lower bundle
(ledger sha df14a654..., verified by the U1 integrity auditor against
the file itself). Forward rule (standing): every "unchanged" claim
carries a hash; every ledger ships a diff or a hash-anchored
sanitized diff against its named predecessor.

## Carried permanent kills (U1 gate finding F10 repaired)

1. kill #2 — scalar fixed return does not force neutral gain. Not
   consumed: the U1E live chain contains no neutral-gain step.
2. RETRACTION-BLOCK-BEGIN — kill declaration: kill #12 — raw-gain
   substitution into F(q) forbidden; F consumes the normalized
   K-transport gains only. BYPASSED, not engaged: the strictness step
   (proof §3) never invokes F(q), q_ret, or the endpoint
   quarter-ceiling mechanism. RETRACTION-BLOCK-END
3. RETRACTION-BLOCK-BEGIN — kill declaration: the unconditional
   endpoint product g(1)g(-1)=0 remains KILLED; its retraction notice
   is restored to the G1 extract inside a marked retraction block
   (see entry 6). RETRACTION-BLOCK-END

## U1 → U1E repair entries (each maps to a U1-gate finding)

4. R1A/R1B (findings F1/F7, the KILL; also F6/E1): strictness
   rho± = q* < 1 established by the Cesàro/selected-multiplier
   identification, TWO independent routes (proof §3.2 primary via
   the Cesàro endpoint-label formula + carrier orientation; §3.3
   confirmation via monotone-tail convergence + selection +
   multiplier continuity). E1 discharged with the exact labels ±t*.
   The certified q* bracket is consumed for the EXACT carrier only —
   the scope the hostile correction audit retained as live.
5. R2 (findings F2/F3/F4): carrier structure re-anchored to the
   promoted public Theorem-(S) certificate (§10 amplitudes, §12
   blocks; S1 referee Gate 5), with real repository/release/DOI
   provenance and current content hashes (incl. the 2026-08-07
   STATUS.json reconciliation, public commits 7cc1a9b and 0292314).
   The self-hash anchor is eliminated; the scalar-orbit document is
   NOT consumed.
6. G1 extract (findings F5/F11/F23): the U1 extract had deleted the
   source's retraction notice because the former hygiene guard
   blacklisted the literal bundle-wide. The retraction notice is
   RESTORED inside a marked retraction block appended to the G1
   dependency copy of that round (the extract; since superseded by the
   FULL source and archived, with stamp, as
   audit_archive/G1_PROMOTED_UPPER_RECEIPT.EXTRACT.SUPERSEDED.md —
   entry 19), and the hygiene guard
   is rewritten to scan the live chain only with retraction blocks
   exempt. This entry is the ledger record of the extract edit the
   commission required.
7. R3 (findings F9/F10/F11): error accounting made explicit —
   principal-compression flux identity inserted; three-row
   bookkeeping table (cut bonds / retained endpoint diagonals /
   unchanged remainder); endpoint-diagonal payment
   lambda_a²+lambda_b² <= R_max·B_I with R_max named; C_B := 1/2 +
   12·R_max derived from the fixed Bell coefficients (marginal
   |.|-sum 4, eight joint terms) and projection-contraction; T_I
   derived by the three-line geometric estimate (mis-citation
   deleted).
8. R4 (findings F13/F14/F15-asymptotics): S_d and D_upper defined
   with the no-dilation convention; unsupported tail-classification
   language struck (the U1a "trichotomy" source claim was FALSE — no
   such classification exists in any source; recorded here as a
   stage-report error and governed by the new quote-with-file-and-
   line rule); floor allocation gives exact dimension for EVERY
   sufficiently large d; two-tail balance algebra written; limsup
   form of the final theorem.
9. R5 (findings F15/F16/F17/F19/F20/F21/F22/F23): banner corrected
   (no predecessor text is preserved — hash-only identification);
   gate rule stated; STATUS updated; U1B_HYGIENE.txt (unbacked
   banner file) deleted; the small-d fixture DEMOTED to
   artifacts/small_d_demoted/ with a full disclosure (argmax window
   rule, non-nested chains, unrecomputed fixture hash, newline
   defect) — it is [V] non-load-bearing; the second-engine
   construction guard (symbolic projectors at arbitrary angles) is
   adopted into guards/; the hygiene guard rewritten (scoped,
   fail-capable, real predecessor check when the public tree is
   present); the strictness/retrodiction guard added.

## Retained from U1 (unchanged, gate-verified sound)

The lambda_j -> 0 ratio argument for rho± <= 1 (F5-verified); G1
conditional-only consumption (F6-verified); exact d = |I| typing with
the historical +3 padding eliminated (F12-verified); the dependency
copies (the v22 distorted-return copy — byte-identical to
predecessor, retained although the live chain no longer consumes the
distorted-return functional; MOVED to audit_archive/ in the U1G
round, ledger entry 19) and the Cesàro/truncation receipts.

## U1E gate round (DENIED) → U1F repairs, 2026-08-07

10. U1E-gate KILLs (proof A1/A2; integrity F-6): the q* bracket was
    unreceipted and the four §3 authorities were prose-anchored. U1F
    ADOPTS THE AUDITOR-SUPPLIED A8 ROUTE as primary strictness
    (elementary Bellman-band argument; anchored inputs only: the
    certificate §1/§10-§12/(1.1)-(1.2), G1 corridor, Cesàro rho
    definition, certified S > 1/4). The sextic, wall-comparison
    selection, equality-module, q* bracket, and h = r^{-2} authorities
    are ALL RETIRED from the live chain. S > 1/4 is now CONSUMED
    (declared; the U1E graph's contrary claim is corrected).
    RETRACTION-BLOCK-BEGIN — kill declaration: kill #12 remains
    bypassed: no F(q), no return-sector mechanism; the quarter-ceiling
    ALGEBRA used is the Lean-checked scalar lemma. RETRACTION-BLOCK-END
11. Proof A4/A5 (pairing identity; monotonicity warrant): discharged
    in U1F §3.1 via the certificate §1 "one-to-one strictly
    increasing" quoted input, stated explicitly as the discharge of
    the Cesàro source's §9 warning. The lossy Cesàro EXTRACT is
    REPLACED by the full source document (faithful predecessor copy,
    incl. its §9 limitation and §2 flux identity) — the F23 pattern
    (extract deletes the source's claim-limiting notice) had recurred
    in that extract (proof A4, integrity F-6a); closed by carrying
    the full source.
12. Integrity F-7/F-8: SELF_AUDIT_U1.md moved to
    audit_archive/SELF_AUDIT_U1.SUPERSEDED.md; U1_GUARD_RESULTS.txt
    moved to audit_archive/*.HISTORICAL.txt; orphan __pycache__
    removed. Integrity F-11 / proof A17: the two-routes commission
    note moved to artifacts/commission_history/ (its numerics were
    computed on a 9-digit rounded bracket and disagree with the
    exact-endpoint values at the 11th digit; superseded by §3.5's
    consistency note). Proof A16/A18: "9e-11" corrected to 8.9e-13;
    the retrodiction RELABELED "consistency note, no evidential
    weight" — the scout and the bracket endpoint are the same
    computation (provenance entanglement, not confirmation).
13. Integrity F-1/F-2/F-3: entry-5's "scalar-orbit document is NOT
    consumed" was FALSE and is retracted — the promoted certificate
    doc IS the scalar-orbit-extraction document; U1F §1 names it as
    such, and discloses the certificate's own Sections-6-9
    residual-risk note, now also carried in the public proof-doc
    header (public commit e50bfec; new hash 090aeceb...).
14. Integrity F-4: banner corrected — dependencies/ DOES carry
    faithful predecessor copies; only audit_archive/ and audit_diff/
    are hash-only. Proof A13: C_diag safe bound 20 adopted (5.5').
    Proof A10: the collar parenthetical struck (labels lie in
    interior Z; compactness suffices). Proof A19: §0 corrected
    (three measurements per party; operator displayed).
15. SCOPE-COINCIDENCE RULE adopted (integrity process note): the
    hygiene guard's scan set is now generated to coincide with the
    banner's live-chain definition.

## U1F gate round (DENIED, both surfaces) → U1G, 2026-08-07

Round-3 verdicts: proof surface — R3 FAIL (custody), R1/R2
PASS-WITH-CONDITIONS, R4 PASS, closing assessment "the mathematics is
sound and I could not break it"; integrity surface — S1/S2 FAIL,
S3/S4 PASS-WITH-CONDITIONS, structural diagnosis "every claim
certified by an instrument whose scope is narrower than the claim."
Both verdicts ON DISK in audit_archive/. U1G responds structurally:
a fully SELF-CONTAINED proof and a minimal live chain, instead of a
fourth patch layer.

16. CUSTODY (proof F21/B1, the fatal blocker): the round-2 proof
    verdict had never been written to disk — it existed only in the
    session record. RECONSTRUCTED verbatim from the session transcript
    (line 50417, duplicate at 50419) with a disclosed provenance
    header: audit_archive/VERDICT-U1E-AUDITOR-1-PROOF.md (sha256
    9ec4fa7037037d281bfa4f45f989dd18b8dcde9c44a6fce6f11f1510e9f536e5).
    It contains findings A1–A22 and residual blockers 1–6; the ledger
    paraphrase in entries 10–15 is now checkable against the source.
    Round-3 verdicts written to disk the day they were delivered
    (VERDICT-U1F-AUDITOR-1-PROOF.md, VERDICT-U1F-AUDITOR-2-
    INTEGRITY.md). STANDING RULE ADOPTED: every gate verdict is
    written to disk before the next round is commissioned.
17. SELF-CONTAINMENT (proof F1/F2/F3/F4/F7/F9, findings 5/6): the U1G
    proof replaces incorporation-by-reference entirely — §§4–7 are
    written in with the corrections (safe C_diag 20; two-sided modulus
    2 per joint diagonal). The two-index outward-ratio framing is
    DROPPED (F2): both ends are bounded separately through the
    one-step ratio limits, which exist by transport-law continuity
    (F4), so the §9 pairing identity of the Cesàro source is not
    consumed at all and its warning needed no discharge. Monotonicity
    is written from the certificate's §10:792 boxed single-map law and
    §6:446 strict monotonicity, quoted with lines (F1/B4). The scalar
    band bound (every t) and the eigen-row limit equation (t_± only)
    are cleanly separated (F3). The harmonic-mean monotonicity step is
    written (F7). All upper-bound displays are rounded UP and agree
    digit-for-digit with the banner, STATUS, and the guard's two-sided
    brackets (F5, integrity finding 8). The former U1E §6.2 q*
    parenthetical is dead with the incorporation that carried it (F9).
    The U1F §3.5 consistency note is DELETED from the live proof
    (integrity finding 5): the U1G proof contains no retraction block
    and no retired numeric; the retirement/consistency record lives in
    this ledger and the on-disk verdicts.
18. LEAN ANCHOR (proof F10/F11/F12, B2/B3): the kernel is anchored
    like any other authority — public commit 6e6adb5, per-file sha256
    in proof §1b, AxiomCheck covering ALL 27 theorems (standard axioms
    only), RateCores.lean committed. The band algebra was ADDED to the
    kernel (band_identity, s_mul_one_sub_s_le_quarter,
    band_quarter_ceiling, amplitude_b_le_half) so the statements the
    proof cites ARE the Lean statements (F12). quarter_lt_window_lower
    is scoped honestly as the literal rational comparison only; the
    window certification is anchored separately (signed statement,
    hash-pinned); and PART B's d=24/d=33 exhibited values are PROMOTED
    to load-bearing for the single fact S > 1/4, as the round-3
    auditor directed (F11/B3) — with the PV-padding construction
    caveat cited to artifacts/small_d_demoted/DISCLOSURE.md by name
    (integrity finding 13).
19. FULL-SOURCE PROVENANCE (proof F17/B5; integrity findings 2/3/11):
    G1 and the truncation source are now FULL byte-identical sealed
    v28.1 copies (G1's own "PROVED CANDIDATE" status line preserved
    and quoted in proof §1c — the extract that deleted it is stamped
    and archived); 08_ENDPOINT_RECEIPT_PROVENANCE.md is carried and
    hash-anchored (G1's endpoint-reserve provenance, finding 11); the
    two U1-era extracts are stamped SUPERSEDED in audit_archive/. The
    Cesàro source's §6 rival strictness (Sprint-1198, prose-named
    supplier), §9 symmetry warning, and §3 |I|+3 padding are
    explicitly NOT CONSUMED / SUPERSEDED by section number in proof
    §1d (integrity findings 4 and 15). The v22 distorted-return copy
    is moved to audit_archive/ as kill provenance — dependencies/ now
    contains ONLY consumed sources.
20. INSTRUMENTS (proof F6; integrity findings 1/5/6/7, blockers
    1/2/8): the hygiene guard GENERATES its authored scan set and
    cross-checks it against the banner's LIVE CHAIN list (H0 —
    scope-coincidence implemented, not just adopted); retraction
    blocks are exempt only after CONTENT VALIDATION (retraction
    language required, live-assertion markers forbidden); the
    kill-route token scan covers every authored live file (H2
    widened); the predecessor hashes are PARSED from proof §1, never
    hand-maintained, and H5 HARD-FAILS when either anchored tree is
    absent; a dangling-pointer check is added (H6); the strictness
    guard's 1/kappa_eff assert is two-sided (F6) and a display-
    concordance check ties the proof's decimals to the guard's values
    (G5). The retired-authority retrodiction guard is moved out of
    guards/ to artifacts/commission_history/ (F20). An INJECTION
    SELF-TEST ships in the bundle (guard_selftest_injection.py) and
    demonstrates each guard firing on the round-3 auditor's own
    injection set.
21. TEXT/STAMP SWEEP (integrity findings 9/10/12/13/14, blockers
    6/7): the dependency graph is REWRITTEN clean (the "independent of
    (S)" claim corrected against Theorem-S §14 — G1 and (S) share
    promoted upstream receipts); in-file SUPERSEDED/gate-DENIED stamps
    added to the U1 proof, U1E proof, U1F proof, archived self-audit,
    and archived guard results (path-only relocation was
    insufficient); STATUS's regenerating-outputs sentence corrected
    (the demoted small-d fixture files ARE manifest-sealed guard
    outputs, disclosed as such); README rewritten (four guards plus
    the self-test); SOURCE_HASH_ANCHORS.json note reconciled with the
    full-copy provenance model; the stale "if U1 promotes" wording in
    the promoted-lower receipt updated to gate-neutral form.

## U1G round-4 gate → round-5 repairs, 2026-08-07

Round-4 integrity verdict (on disk,
audit_archive/VERDICT-U1G-AUDITOR-2-INTEGRITY.md): DENIED — 14/15
round-3 findings verified discharged; the auditor replayed AxiomCheck
(27/27) and cmp-verified all copies; the denial is "narrow and
mechanical": two content channels exempt from instruments plus one
instrument missing its load-bearing assert. "If round 5 ships
blockers 1-4 with the injections attached, I expect this to promote."

22. ROUND-4 BLOCKER EXECUTION (findings 1-8, blockers 1-6):
    (1) PART B now ASSERTS the exhibited fact (values at d=24 and
    d=33 strictly exceed 1/4 — fail-capable); its false "d >= 6"
    docstring corrected; the Jacobi-quotient-to-Bell-value bridge
    anchored by path + hash in proof §1b (the public sprint-1292
    certificate document). (2) H7 added: every dependency copy is
    verified BYTE-IDENTICAL against its hash-anchored EXTERNAL sealed
    source (v28.1 bundle / public certificate / consolidated bundle),
    hard-fail if a source tree is absent; the auditor's J1 injection
    (tamper + full internal re-pin) ships as selftest I9. (3) The
    retraction-block exemption is now a REGISTRY (banner: 1 block,
    ledger: 3 blocks, everything else including copies: zero),
    content-checked; the auditor's J2 "Historical Lemma" evasion and
    J10 copy-block ship as selftests I10/I11. (4) H8 added: manifest
    re-verified against the filesystem in full and partitioned by the
    banner's AUTHORED/COPIES/HISTORICAL classification — the
    auditor's J3 (non-.md authority file) and J4 (unscanned
    directory) ship as selftests I12/I13. (5) PART A's source
    ENDPOINT_PROJECTOR_TRUNCATION_CONSTRUCTION.md carried as the
    SIXTH full byte-identical copy (matches the U1-round hash anchor
    d486e3e3...). (6) Cosmetics: graph legend conditional-typing
    note; AxiomCheck stdout receipt stored
    (audit_archive/AXIOMCHECK_RECEIPT_2026-08-07.txt); manifest added
    to the change list; Cesàro §3 range corrected to 101-132.

23. ROUND-4 PROOF VERDICT EXECUTION (on disk,
    audit_archive/VERDICT-U1G-AUDITOR-1-PROOF.md: DENIED — R3 scope
    PASS; "On the mathematics I have nothing left to attack"; fourth
    consecutive round with no mathematical kill). Its four blockers:
    (F-01, CUSTODY — ACKNOWLEDGED AS A PROCESS VIOLATION) the
    adjudicating track edited the bundle while the round-4 auditors
    were reading it; the mid-round edits were correct in content and
    wrong in procedure; the FREEZE RULE is adopted as standing (see
    banner): no write between commissioning and verdict, every round
    commissioned against a stated frozen commit; all round-4-era
    repairs land here, as round-5 content. (F-02, THE FALSE PRINTED
    INEQUALITY) §3.7's chain used the short down-rounded surrogate
    K = 0.0836782 in a DENOMINATOR, where down-rounding is unsafe:
    2/K = 23.9010877... overshoots the displayed bound — false as
    printed even though the theorem's constant is true (auditor
    verified at 80 digits). REPAIRED: K_0 := 0.08367827985 (safe both
    ways: 2/K_0 = 23.90106493088... <= 23.9010650 and
    K_0/2 >= 0.0418391,
    both verified in exact rationals), with the rounding-direction
    logic stated in §3.7; and G5 is now a CHAIN CHECK — it parses the
    proof's own displayed K and re-verifies the displayed chain in
    exact rationals (the auditor's S-I4 ships as selftest I14, with a
    baseline I14b). (F-03, LEAN ANCHOR AT THE WRONG OBJECT) §1b's
    I3322Kernel.lean hash was a working-tree CRLF hash, not the
    committed blob. REPAIRED: all six §1b hashes are now COMMIT-BLOB
    digests, §1b says so and names the verification command, and H5
    verifies via git cat-file against commit 6e6adb5 — never the
    working tree. (F-04, PROVENANCE TERMINATION) the 08 document's
    family-A source REFLECTION_DUAL_UPPER_ENVELOPE_AND_ADAPTIVE_TAILS
    .md is carried as the SEVENTH full byte-identical copy, and §1c
    now states plainly where the chain terminates: the sealed v28.1
    closeout archive (sha 9ec7cab5...), inherited at PROMOTED status.
    Recommended items adopted: H9 fencing-phrase check (forbidden
    unfencing language in the proof; required fencing sentences —
    the auditor's H-I2/H-I5 class ships as selftest I15); PART B
    Rayleigh-quotient rigorous lower bound added to the exhibition
    assert; the d(x,u) cost-function definition cited by file and
    line and hash-anchored (sprint-1197); AxiomCheck stdout receipt
    already stored under entry 22.

## U1G round-5 gate → round-6 seal, 2026-08-07

Round-5 verdicts (on disk): PROOF surface — **PROMOTE** at frozen
commit 5c3e9c8b, the first passing surface in five rounds ("I set out
to deny this. I could not."), with two LOW documentation notes and
four instrument-coverage recommendations; INTEGRITY surface — DENIED
with six mechanical blockers ("Ship blockers 1-6 with the nine
injections attached and freeze it again, and I expect this to
promote"). The freeze HELD byte-for-byte through both audits — the
round-4 custody violation did not recur.

24. ROUND-5 BLOCKER EXECUTION (integrity findings 1-9 + proof
    recommendations M-01/M-02/D-01/I-01..I-04):
    (1) H7 is now DIGEST-ANCHORED: a registry of pinned sha256 values
    in the guard's code, checked against BOTH the copy and its
    external sealed source — the lockstep-tamper injection N1c ships
    as selftest I16 (with an external-mirror control I16a so the real
    sealed bundles are never touched). The banner's H7 wording now
    matches the implementation. (2) Retraction blocks are pinned by
    CONTENT digest per block (RB_CONTENT_DIGESTS) — the
    interior-edit laundering injection N3 ships as I17. (3) The
    partition is closed by a COMPLETE pinned filename allowlist
    (ALLOWED_FILES) covering every directory including artifacts/,
    guards/ and the audit trees; the round-4 J5 injection finally
    ships as I18, the artifacts-poisoning N2 as I19; the __pycache__
    exemption is narrowed to *.pyc only and disclosed (N8 ships as
    I20). (4) G5 verifies EVERY K_0 occurrence (N4 ships as I21) and
    adds a STRUCTURAL range check on every <=/>= display of the
    tracked constants (the false-tighter-display injection AI-4 ships
    as I23). (5) The DISCLOSURE.md contradiction is resolved (PART B
    load-bearing for exactly one fact, everything else unchanged);
    H9 is extended to every authored live file (N5 ships as I22) with
    the AI-3 paraphrase set added; round labels corrected to round 6.
    (6) The last three verdicts are brought inside the bundle
    (round-1 pair + round-2 integrity, faithful copies in
    audit_archive/) and the blind-batch-v19 originals are committed
    to git in this seal — every verdict of this gate is now
    hash-pinned in the manifest and in git. Proof-surface notes:
    M-01 truncation-convention display fixed at all three sites;
    M-02 the decimal surrogate renamed K_0 (the compact set keeps K);
    I-03 H5 now parses the Lean commit id from the proof (AI-6b ships
    as I24). Selftest total: 33 executed cases across the round-3/4/5
    injection sets (count corrected per round-6 finding 9c).

## U1G round-6 gate -> round-7 seal, 2026-08-07

Round-6 verdicts (on disk): PROOF surface (delta audit) - **PROMOTE
CARRIES** to a52d24aa (zero mathematical, zero custody, zero
moderate-or-above findings; 19 sandbox mutations all behaved);
INTEGRITY surface - DENIED, the narrowest yet ("the gap between this
bundle and promotion is now three regex edits, two scan-set
decisions, and a cmp"; zero false displays found in the frozen live
chain under all six comparison symbols). Freeze held on both audits.

25. ROUND-6 BLOCKER EXECUTION (integrity findings 1-9, blockers 1-7;
    proof-delta findings F-01/F-02/F-03):
    (1) G5's identifier regex widened to accept BOTH the pre-rename
    and post-rename surrogate names (the round-6 mechanism lesson:
    never change an identifier in the same seal that hardens a guard
    against it) - the V1 verbatim-N4 injection ships as I25.
    (2) G5's structural check accepts all six comparison syntaxes
    (ASCII strict/non-strict + Unicode) - V2/V3 ship as I26/I27.
    (3) The mu_min range branch is wired - V4 ships as I28.
    (4) N2c partially closed + disclosed: README.md and
    U1_TO_U1E_CHANGES.md (no legitimate killed content) are now
    token-scanned by H9x - E1 ships as I29; the historical files
    remain unscanned-by-design with the disclosure and its control
    (change-list-vs-git-diff) stated in the banner.
    (5) The *.pyc exclusion is scoped to __pycache__ directories so
    the code matches its comment and the banner rule - E4 ships as
    I31.
    (6) H10 custody cmp added: the three early-round verdict copies
    are byte-compared against their blind-batch-v19 originals on
    every run - E3 ships as I30.
    (7) Documentation: ledger entry-23 M-01 site corrected (the
    fourth site the round-6 auditors found; 2/K_0 = 23.90106493088...
    truncation convention); proof section 3.7 prose rename completed
    (the two residual bare-K occurrences); the RB "anywhere else"
    banner sentence scoped to the live chain; entry-24 selftest count
    corrected. Selftest total after this round: 40 executed cases
    across the round-3/4/5/6 injection sets.

## U1G round-7 gate — PROMOTED, 2026-08-07

Round-7 verdicts (on disk): PROOF delta — PROMOTE CARRIES to
bf47d0b8 (word-level diff: exactly two tokens, the sanctioned F-02
rename; chain re-verified in exact rationals; G5 diff audited for
weakening — strengthening on every axis). INTEGRITY — **PROMOTE**:
all seven round-6 blockers executed as written and verified by
injection; the round-6 transcriptions authenticity-tested against
the reconstructed a52d24aa tree (every falsifiable claim reproduced);
the disclosed N2c control run and verified exact; 30 injections;
ZERO false displays in the frozen live chain at 80 digits. THE
FROZEN RULE IS SATISFIED — see PROMOTION_RECORD.md.

26. PROMOTION EXECUTION (round-7 recommended items): README rewritten
    with accurate counts (F1 — it had said "fourth gate round", "five
    copies", "six verdicts"; the entry-point document now matches the
    bundle it introduces); banner header and verdict shelf brought to
    round 7 / gate-closed (F2/F3); the CRLF working-tree-digest
    reproducibility caveat DISCLOSED in the README with the
    git-ls-tree verification alternative (F9); the *.pyc shorthand
    noted as stricter-in-code (F10). F4-F8 (whitespace-exact
    identifier regex, unpinned S_LO display, unbranched safe-direction
    ranges, syntax ceiling, git-only custody for the later verdicts)
    are recorded as ACCEPTED PERIMETER LIMITS per the round-7
    verdict's calibration — none load-bearing, none touching the
    promoted claim, each bounded by a pinned token, a disclosed
    control, or git custody at frozen commits.
