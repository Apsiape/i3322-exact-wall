# P5 Artifact Closeout — v28

1. The exact rational endpoint verifier is shipped verbatim and replayed by the guard runner.
2. The already-blind-verified G1 endpoint-positivity/collar document and its exact arithmetic guard are included as live dependencies.
3. Endpoint-projector truncation remains supplementary upper-only material.
4. Commission v28-L is lower-bound-only and diff-scoped.
5. `RHO_Q_IDENTIFICATION.md` uses endpoint labels with `_end` subscripts and distinguishes them from normalized response gains \(\alpha,\beta\).
6. The live R1 copy uses `r_A^{ret},r_B^{ret}` for fitted residuals and `r_{A,mult},r_{B,mult}` for multiplier functions.
7. `TOP_D_SIMULTANEOUS_MONOTONE_CLUSTERING_V23.md` is classified as **R1-only optional provenance** because the historical R1 section 3 consumes top-d deletion; it is not consumed by the main v28 neutral-return route.
8. Detached member-by-member SHA-256 manifest and archive SHA-256 are generated only after the final guard pass.
9. No numerical upper coefficient and no \(\Theta(\log)\) claim can be promoted by this package.
