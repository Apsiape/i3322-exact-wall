# Manifest note — v28

The detached SHA-256 manifest is generated after all v28 guards pass. It records every sealed archive member by relative path, size, and SHA-256. The archive SHA-256 is stored in the adjacent `.sha256` file. No self-hash is embedded inside the archive.
