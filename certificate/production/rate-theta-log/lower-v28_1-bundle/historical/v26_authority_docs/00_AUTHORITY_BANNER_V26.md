# I3322 v26 Lower-Bound Re-Audit — Authority Banner

**Date:** 2026-08-06  
**Status:** **LOWER-BOUND THEOREM CANDIDATE — NOT PROMOTED**  
**Promotion scope:** only
\[
S-S_d\ge c(1+d)^{-K}e^{-Cd}
\]
and therefore
\[
D_{\rm lower}(\varepsilon)=\Omega(\log(1/\varepsilon)).
\]

The promoted qualitative Theorem (N) remains untouched.

## Live v26 route

The v25 CME/component route is retired. The v26 lower route uses:

1. a reflection-symmetric scalar grid of width \(\eta\asymp\varepsilon^{1/8}\);
2. deletion of a weighted cover of **far-inverted raw joint cells**;
3. the new raw-cell parity edge budget \(|E_{\rm cell}|\le4d\);
4. same-marginal response localization plus an explicit joint-block X/U mass bridge;
5. a simple marginal-cell walk ending at a sink or repeated parity cell;
6. a dimension-free repeated-cell cycle payment by state-carrying exactification;
7. the exponential branch/Green comparison and eighth-power inversion.

No near-conflict component diameter, pointwise finite-n \(\tau\), C034 atlas, historical normalization, component rank-cost sum, or robust packet Bell-localization theorem is used.

## R1 authority

`PROJECTED_RETURN_MISMATCH_R1_V23.md` is restored as **LIVE, DISTORTED-RETURN SCOPE ONLY**:
\[
(1-q)z\le e_{\rm ret}.
\]
It services \(q<889/1000\) and may be used as an early terminal payment. It is **not** used to infer neutral gain and is not needed for the main v26 repeated-cell contradiction.

## Promotion rule

The lower bound promotes only if all six gates in `10_LOWER_BOUND_PROMOTION_AUDIT_COMMISSION.md` receive **PASS**. The constructive upper bound is explicitly out of scope for this promotion decision.
