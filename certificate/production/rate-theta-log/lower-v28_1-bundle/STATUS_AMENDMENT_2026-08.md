# Status amendment (2026-08, append-only)

`STATUS_V28.json` in this bundle records the pre-promotion status
(`LOWER_BOUND_V28_1_FINAL_FIXES_READY_FOR_CLEAN_VERIFICATION`, dated
2026-08-06) and is retained unchanged as the frozen record of the bundle
at commissioning. The clean verification it names was subsequently
executed and passed under the frozen rule, and the promotion it
conditions on fired:

- Promoted statement: `S − S_d ≥ c(1+d)^(−K) e^(−Cd)` at local-dimension
  scope (existential constants), hence `D_lower(ε) = Ω(log 1/ε)`.
- Promotion receipt: `../upper-U1G-bundle/PROMOTION_RECORD.md`, whose
  "SECTION-8 CONDITIONAL FIRES" paragraph consumes this lower bundle's
  promoted statement ("the previously promoted lower bound
  D_lower(eps) = Omega(log(1/eps)) (v28.1 closeout, five external
  rounds)") in closing `D(ε) = Θ(log 1/ε)`.

This file is an append-only pointer added so the promotion status is
discoverable from inside the lower tree; no record in this bundle is
edited.
