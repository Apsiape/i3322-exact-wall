# Pre-registration: eliminate the amplitude-fibre gate

The event-measure route will be useful only if the scalar quarter wall can be
stated directly in the two coordinates that the measure transports.  Before
running a guard, register the target

```text
m_0 <= 84 |a(u)+u| + (17/10)|h(u)|,
m_0=q_*-1/4,
```

on the certified active contact graph, where `a(u)=P^-1(-P(u))` and
`h(u)=log C(u)` is the composed log-resolution translation.

The proof must use only the already-certified Bellman contact, response
weight box, predecessor derivative box, and weighted closure matrix.  It may
not choose common Hilbert target vectors or assume that response-specific
multiplicity fibres coincide.

Falsifiers:

1. the reflected contact remainder is not Lipschitz-controlled by
   `|a(u)+u|` on the active box;
2. the two response ratios are not controlled by `h(u)` and the same order
   mismatch;
3. the claimed rational constants do not dominate the exact algebra; or
4. the proof silently assumes exact finite support closure.

